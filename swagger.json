{
  "swagger": "2.0",
  "info": {
    "description": "This is an API Swagger to get api info",
    "version": "1.0.0",
    "title": "Swagger API"
  },
  "host": "http://localhost:3001",
  "basePath": "/",
  "tags": [
    {
      "name": "Login",
      "description": "Login API routes"
    },
    {
      "name": "Register",
      "description": "Register user CRUD"
    }
  ],
  "schemes": ["http", "https"],
  "paths": {
    "/auth" : {
      "post": {
        "tags":["Login"],
        "summary": "login user on the plataform",
        "description": "Log in user and return a token for authorization",
        "operationId": "loginUser",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "description": "Login object that needs to be added",
            "required": true,
            "schema": {
              "$ref": "#/definition/Login"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Succesful Operation",
            "schema": {
              "$ref":"#/definition/ResponseLogin"
            }
          },
          "400": {
            "description": "Bad Request"
          },
          "403": {
            "description": "Forbidden"
          }
        }
      }
    },
    "/user": {
      "post": {

      },
      "get": {

      }
    },
    "getAll": {
      "get": {
        
      }
    }
  }
}
